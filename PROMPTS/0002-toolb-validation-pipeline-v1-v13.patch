--- a/ToolB_AI_Agent_Pattern_Generator_Prompt_v1.md
+++ b/ToolB_AI_Agent_Pattern_Generator_Prompt_v1.md
@@ -512,7 +512,7 @@ After receiving a parseable JSON response from the agent, ToolB runs the **same
 ## Output Validation Pipeline
 
-After receiving a parseable JSON response from the agent, ToolB runs the **same validation rules as ToolC** (V1–V10) before writing `pattern.json`.
+After receiving a parseable JSON response from the agent, ToolB runs the **same validation rules as ToolC** (V1–V13) before writing `pattern.json`.
 
 Validation order:
 1. JSON schema structure validation (jsonschema)
@@ -523,13 +523,22 @@ Validation order:
 6. Each signal name exists in JSONL `signal_frequency_table` or `custom_helper_registry`
 7. `envelope.example` keys are subset of `keys_all_of + keys_any_of`
 8. `framework` matches JSONL `global_stats.framework.detected` (warning only)
 9. Template names are unique
 10. `priority_order` ends with `"default"`
+11. Any top-level key beginning with `_` is silently stripped before schema validation (extension field passthrough — allows `_tool_b_meta` to be present during the correction retry flow without causing a spurious V1 failure). **Do not raise an error for extension fields.**
+12. `(thresholds.endpoint - thresholds.uncertain)` must be ≥ `minimum_threshold_gap` from JSONL `pattern_json_generation_hints` (default: 10 if field absent). Warning only — do not abort.
+13. `pattern.json` field `source_jsonl_schema_version` must equal `"2.0"`. Warning only if missing or mismatched.
 
 On failures:
-- Hard errors (V1–V7, V9–V10): trigger correction retry (Scenario 2)
-- Warnings (V8): log and continue
+- Hard errors (V1–V7, V9–V10): trigger correction retry (Scenario 2)
+- Warnings (V8, V12, V13): log and continue — do not trigger retry
+- V11: silent strip — never an error or warning
+
+> **Implementation note — V11 ordering**: Strip `_`-prefixed top-level keys from the parsed JSON object **before** passing it to `jsonschema.validate()`. This must happen as the very first step of the validation pipeline. `_tool_b_meta` is appended **after** the entire validation pipeline completes — it must never be present in the object being validated.
 
 After successful validation, append a `_tool_b_meta` field to pattern.json:
